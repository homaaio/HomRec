## After removing all the .sh scripts, there's nothing left to write in this documentation :(
 New text and documentation will appear after adding .bat and .sh scripts (for each system, respectively).

## I don't see the point in creating scripts for Linux builds now, given that the program doesn't support Linux.